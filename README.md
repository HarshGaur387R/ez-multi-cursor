# ez-multi-cursor

Multi-cursor editing plugin for Neovim.

## Installation

### Using Lazy:

```lua
{
    "HarshGaur387R/ez-multi-cursor",
    config = function()
        require("ez-multi-cursor").setup()
    end,
}